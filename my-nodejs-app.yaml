apiVersion: platform.helios.io/v1
kind: HeliosApp
metadata:
  name: my-nodejs-app
  namespace: default
spec:
  # ===== Your Application Source Code =====
  gitRepo: "https://github.com/PhuocHoan/helios"
  gitBranch: "main"

  # ===== Container Image =====
  imageRepo: "docker.io/hophuochoan/helios"

  # ===== Application Settings =====
  port: 8080
  replicas: 3

  # ===== CI/CD Pipeline =====
  pipelineName: "from-code-to-cluster"
  serviceAccount: "pipeline-sa"
  webhookSecret: "github-webhook-secret"
  pvcName: "shared-workspace-pvc"

  # ===== GitOps Template (optional - use default or custom) =====
  templateRepo: "https://github.com/PhuocHoan/helios-template"
  templatePath: "templates/simple-app"

  # ===== GitOps Destination =====
  gitopsRepo: "https://github.com/PhuocHoan/helios-gitops"
  gitopsPath: "apps/dev/helios"

  # ===== Context Subpath (where Dockerfile is located) =====
  contextSubpath: "my-nodejs-app"

  # ===== Custom Values =====
  values:
    environment: "dev"
    imageTag: "latest"
